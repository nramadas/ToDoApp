<div class="new_task">
  <%= form_for(@task) do |f| %>
    <p>
      <%= f.label :title, "What do you want to do?" %>
      <%= f.text_field :title %>
    </p>
    <p>
      <%= f.label :body, "Any specifics?" %>
      <%= f.text_area :body %>
    </p>
    <p>
      <% @task.tags.each do |tag| %>
        <%= tag.tag_name %>,
      <% end %>
      <%= label_tag :tag, "Add tag" %>
      <%= text_field_tag :tag %>
    </p>
    <p>
      <%= f.submit %>
    </p>
    <p>
      <%= link_to 'Cancel', user_path(current_user) %>
    </p>
  <% end %>
</div>